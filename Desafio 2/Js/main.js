let nombre = prompt ("Ingrese su nombre");
let numeroIngresado = prompt ("Ingrese un numero entre 10 y 50")

if ((numeroIngresado >= 10) && (numeroIngresado <=50)){
    alert (nombre + " Felicidades, usted acertó el numero correcto ");
} else {
    alert ("Error, por favor ingrese otro número");
    
}